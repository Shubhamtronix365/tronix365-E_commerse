// ============================================================
// INVOICE PAGE — DISABLED (commented out as per project requirement)
// To re-enable: restore the original Invoice.jsx content and
// add the route in App.jsx:
//   const Invoice = React.lazy(() => import('./pages/Invoice'));
//   <Route path="/invoice/:id" element={<ProtectedRoute><Invoice /></ProtectedRoute>} />
// ============================================================

// Placeholder export so any accidental import doesn't crash the build
const Invoice = () => null;
export default Invoice;
